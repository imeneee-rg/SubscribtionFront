const port = 5000;

const serverurl = "http://localhost:" + port;

export const usersapi = serverurl + "/users";
export const clientsapi = serverurl + "/clients";
export const inscriptionsapi = serverurl + "/inscriptions";
export const productsapi = serverurl + "/products";
export const periodsapi = serverurl + "/periods";
export const pricesapi = serverurl + "/prices";